# config.py for 'eventLogConv_event_based.py' and 'eventLogConv_trace_based.py'
# activate variables: assign 1, deactivate: assign 0


# shuffle traces (change order of traces) of a log-file
activate_shuffle = 0

# transform data in log_file (map strings to ints, min-max-normalize numbers and format timestamp to 'TT.MM.YYYY HH:MM:SS')   
activate_transformation = 0